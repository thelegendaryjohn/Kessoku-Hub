<!DOCTYPE html>

<html>

<head>
  <title><%= title %></title>
</head>

<body>
  <h1>Home</h1>
  <p><%= message %></p>
  <p>Characters:</p>
  <ul>
    <% characters.forEach(element => { %>
    <%- include("partials/characterCard.ejs", { character: element }) %>
    <% }); %>
  </ul>
</body>

</html>